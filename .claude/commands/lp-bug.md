Run the `lp-bug` CLI command with the provided arguments and display the output.

Execute: `lp-bug $ARGUMENTS`

- Run the command using the installed `lp-bug` CLI
- Display the full output to the user
- If the command fails, show the error and suggest corrections
